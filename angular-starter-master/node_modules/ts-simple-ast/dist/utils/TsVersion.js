"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var ts = require("typescript");
var TsVersion = /** @class */ (function () {
    /* istanbul ignore next */ function TsVersion() {
    }
    TsVersion.getMajor = function () {
        return this.versions[0];
    };
    TsVersion.getMinor = function () {
        return this.versions[1];
    };
    TsVersion.greaterThanEqualToVersion = function (major, minor) {
        var currentMajor = this.getMajor();
        var currentMinor = this.getMinor();
        if (currentMajor < major)
            return false;
        if (currentMajor > major)
            return true;
        return currentMinor >= minor;
    };
    TsVersion.versions = ts.version.split(".").map(function (v) { return parseInt(v, 10); });
    return TsVersion;
}());
exports.TsVersion = TsVersion;
